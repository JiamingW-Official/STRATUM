// hexdb.io is currently unavailable — stubs keep the rest of the app working.
// Registration, type, and operator data come directly from the ADS-B poll.
export function queueHexLookup() {}
export function getAircraftMeta() { return null; }
