{
  "name": "stratum",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && wrangler deploy",
    "test:sky": "playwright test tests/sky.smoke.spec.ts",
    "verify:sky": "npm run build && npm run test:sky"
  },
  "dependencies": {
    "three": "^0.183.2",
    "vite": "^7.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "https-proxy-agent": "^9.1.0",
    "terser": "^5.46.1",
    "wrangler": "^4.0.0"
  }
}
